language js;

// Matches the `any` keyword in type annotations.
`any` where {
    register_diagnostic(
        span = $match,
        message = "Using values of type `any` is not supported! Use `unknown` instead."
    )
}
