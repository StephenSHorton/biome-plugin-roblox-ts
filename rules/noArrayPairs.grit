language js;

`pairs($arg)` where {
    register_diagnostic(
        span = $match,
        message = "Do not use Array<T> with pairs(). Key values will not be shifted from 1-indexed to 0-indexed."
    )
}