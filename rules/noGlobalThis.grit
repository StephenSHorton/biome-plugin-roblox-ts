# Don't use 'this' outside of classes or objects
`this` where {
  not within or {
    `class $_ { $_ }`,
    `$_ = { $_: function($_) { $_ } }`,
    `$_ = { $_($_) { $_ } }`,
    `function $_($_) { $_ }`
  }
}