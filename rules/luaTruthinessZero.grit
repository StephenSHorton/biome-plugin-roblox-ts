language js;

if_statement($condition, $consequence) where {
    $condition <: `0`,
    register_diagnostic(
        span = $condition,
        message = "Zero is falsy in TS. If intentional, disable this rule."
    )
}
