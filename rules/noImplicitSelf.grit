language js;

JsLabeledStatement(body = JsExpressionStatement(expression = JsCallExpression())) as $match where {
    register_diagnostic(
        span = $match,
        message = "Use `.` instead of `:` for method calls. Implicit self is not supported in roblox-ts."
    )
}
