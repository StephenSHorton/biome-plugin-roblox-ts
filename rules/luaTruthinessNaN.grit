language js;

if_statement($condition, $consequence) where {
    $condition <: `NaN`,
    register_diagnostic(
        span = $condition,
        message = "NaN is falsy in TS. If intentional, disable this rule."
    )
}
